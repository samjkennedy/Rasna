Int[] sort(Int[] array) {

   Int n = len(array)
   for (Int i = 1 to n) {
       for (Int j = 0 to n - i) {
           if (array[j] > array[j + 1]) {
               Int tmp = array[j]
               array[j] = array[j + 1]
               array[j + 1] = tmp
           }
       }
   }
   return array
}

Int[] arr = [6, 2, 8, 3, 4, 9, 5, 1, 7, 0]
for (Int a in sort(arr)) print(a)

Int[] ints = [1, 2, 3]
for (Int i in ints) print(i)

Bool[] bools = [true, false, true, true, false]
for (Bool b in bools) print(b)

String[] strings = ["Hello", "World"]
for (String s in strings) print(s)

Real[] reals = [1.414, 2.718, 3.141]
for (Real r in reals) print(r)
