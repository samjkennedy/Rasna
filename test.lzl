t: Tuple = ("Hello",)

print(len(t))

//TODO: This will require runtime casting
//s: String = t[0]

for (v: var in t) {
    print(v)
}

print(t[0])
for (i: Int = 0 to len(t)) {
    print(t[i])
}

fn multiReturn(a: Int): Tuple {
    match a {
        1    -> return ("Hello", true),
        else -> return ("World", false, 1)
    }
}

t = multiReturn(1)
print(len(t))