enum Color {
    Red
    Blue
    Green
    Pink
    Yellow
}

enum TrafficLight {
    Red
    Yellow
    Green
}

fn favouriteColour(c: Color) {
    if (c == Color.Red) {
        print("Your favourite colour is red")
    } else {
        print("I don't know that colour")
    }
}

fn main() {
    c: Color = Color.Green

    favouriteColour(c)

//    for (c: Color = Color.Red to Color.Yellow) {
//        favouriteColour(c)
//    }

//    i: Int = c as Int
//    r: Color = 0 as Color

//    s: String = c as String //Gets the name of the enum
}