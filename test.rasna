struct Book {
    page: String
    open: Bool
}

fn Book(): Book {
    return Book{"", true}
}

fn close(ref book: Book): Bool {
    book.open = false
    print("Book closed")
    return true
}

fn main() {
    book := Book()
    book->close()
}